import { Routes } from '@angular/router';
import { PizzaCalculatorComponent } from './pizza-calculator/pizza-calculator';

export const routes: Routes = [
  { path: '', component: PizzaCalculatorComponent }
];
